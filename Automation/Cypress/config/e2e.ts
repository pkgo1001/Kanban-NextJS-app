import 'cypress-xpath';
import './commands';
// Opcional: exponer usuarios en Cypress.env() para usar en specs
// import { getConfig } from './test-config';
// antes de cada spec: cy.wrap(getConfig()).as('testConfig');